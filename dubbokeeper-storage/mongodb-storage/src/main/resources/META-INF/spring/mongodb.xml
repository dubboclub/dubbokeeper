<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd
        http://www.springframework.org/schema/data/mongo http://www.springframework.org/schema/data/mongo/spring-mongo-3.0.xsd">

        <dubbo:service interface="com.dubboclub.dk.storage.StatisticsStorage"
                       ref="mongoDBStatisticsStorage"
                       timeout="${dubbo.monitor.mongodb.storage.timeout}"/>

        <mongo:mongo-client id="mongo"
                     host="${dubbo.monitor.mongodb.url}"
                     port="${dubbo.monitor.mongodb.port}"
                     credential="${dubbo.monitor.mongodb.username}:${dubbo.monitor.mongodb.password}@${dubbo.monitor.mongodb.dbname}"/>

        <mongo:db-factory id="mongoDbFactory"
                          dbname="${dubbo.monitor.mongodb.dbname}"
                          mongo-client-ref="mongo"/>

        <bean id="statisticsDao" class="com.dubboclub.dk.storage.mongodb.dao.StatisticsDao">
                <property name="mongoTemplate" ref="mongoTemplate"/>
        </bean>
        <bean id="applicationDao" class="com.dubboclub.dk.storage.mongodb.dao.ApplicationDao">
                <property name="mongoTemplate" ref="mongoTemplate"/>
        </bean>

        <bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
                <constructor-arg name="mongoDbFactory" ref="mongoDbFactory" />
        </bean>
        <bean class="com.dubboclub.dk.storage.mongodb.MongoDBStatisticsStorage" id="mongoDBStatisticsStorage">
                <property name="applicationDao" ref="applicationDao"  />
                <property name="statisticsDao" ref="statisticsDao"  />
        </bean>

</beans>
